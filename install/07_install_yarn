curl -sL https://dl.yarnpkg.com/debian/pubkey.gpg | sudo -H apt-key add -
echo "deb https://dl.yarnpkg.com/debian/ stable main" | sudo -H tee /etc/apt/sources.list.d/yarn.list
sudo -H apt-get update && sudo apt-get install -y yarn

